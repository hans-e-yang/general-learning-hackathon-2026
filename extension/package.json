{
  "name": "circlr-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run compile && npm run icon && npm run clean",
    "compile": "tsc -p tsconfig.json",
    "icon": "node scripts/icon.mjs",
    "clean": "node scripts/clean.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "typescript": "^5.0.0"
  }
}
